type Dispatchable = string | any


const dispatch = (dispatchable: Dispatchable) => {

        // do dispatch
}


const systemDispatch = (dispatchable: Dispatchable) => {
    // do system
    dispatch('s')
}